import { fetchComment } from "@/lib/getMovies";
import React from "react";

const Comments = ({ movieId }: any) => {
//   const getComment = async () => {
//     const commentBox = await fetchComment(movieId);
//     console.log("2", commentBox);
//   };
  return 
//   (
    // <div>
      {/* Comments
      {getComment} */}
    //   <getComment />
    {/* </div> */}
//   );
};

export default Comments;
